services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: pass123
      POSTGRES_DB: blog_db
    ports:
      - "15432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
volumes:
  db_data: